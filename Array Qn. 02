// 1. Taking an array as an input from the user and print them on the screen.

import java.util.*;
public class Array {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
        int size = sc.nextInt();
        String name[] = new String[size];
        
        //input
        for(int i=0; i<size; i++) {
            name [i] = sc.next();
        }
        
        //output
        for(int i=0; i<size; i++) {
            System.out.println("name " + (i+1) +" is : " + name[i]);
        }
    }
}

//Solution
//input
//2
//Ajeet raj

//output
//name 1 is : Ajeet
//name 2 is : raj
